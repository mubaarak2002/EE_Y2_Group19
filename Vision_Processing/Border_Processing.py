import numpy as np
import cv2 as cv

def Mask(Image, Range):
    #Needs to create a mask of the image with just the white lines

def Parallel_Detector(Masked):
    #Takes a mask of just the line, identifies any two lines segments that are equidistant, linearly co
    #converging towards eachother (used to identify straight segments)

def distance(Masked)
    #Identifies the differnece in distance between any two segments in the image, and translates this into a real time image.